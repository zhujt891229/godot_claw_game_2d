# 🗺️ 地图配置指南

## ✅ 已创建的文件

| 文件 | 说明 |
|------|------|
| `scripts/level_manager.gd` | 关卡管理器 (重生、检查点) |
| `scripts/checkpoint.gd` | 检查点逻辑 |
| `scripts/spike.gd` | 尖刺陷阱 |
| `scenes/level_01.tscn` | 第一关场景 |
| `scenes/platform.tscn` | 平台预制件 |
| `scenes/spike.tscn` | 尖刺预制件 |

---

## 📝 Godot 配置步骤

### 步骤 1: 打开关卡场景

1. 启动 Godot 4.x
2. 打开项目
3. 双击 `scenes/level_01.tscn`

---

### 步骤 2: 配置 TileSet (地形)

#### 2.1 创建 TileSet 资源

1. 选中 `TileMapLayer` 节点
2. 在 Inspector 找到 `Tile Set` 属性
3. 点击 `[New TileSet]`
4. 点击资源图标 (📄) 打开 TileSet 编辑器

#### 2.2 添加瓦片图集

1. 在底部 **TileSet** 面板
2. 点击 `+` 添加 **Atlas Source**
3. 选择你的瓦片图：
   ```
   assets/sprites/Asset/Tilemap/Tilemap_ProjectLittleAdventurerAndie2D.png
   ```
4. 设置 **Tile Size** = `32x32`

#### 2.3 配置瓦片碰撞

1. 在 TileSet 面板选择 **Terrain** 或 **Paint** 模式
2. 选中一个瓦片
3. 在右侧 **Tile Data** 面板
4. 找到 **Physics** 标签
5. 点击 `+` 添加碰撞形状
6. 绘制碰撞区域 (和瓦片一样大)

#### 2.4 保存 TileSet

1. 在 Inspector 点击 `Tile Set` 属性
2. 选择 **Save As...**
3. 保存到 `resources/terrain_tileset.tres`

---

### 步骤 3: 绘制关卡

#### 3.1 选择 TileMapLayer

1. 在场景树选中 `TileMapLayer`
2. 底部打开 **TileMap** 面板

#### 3.2 绘制地面

1. 选择 **Paint** 模式 (画笔图标)
2. 在瓦片选择区选择地面瓦片
3. 在 2D 视图中绘制：
   - 从左侧 (-400, 150) 开始
   - 画一条长平台到右侧 (400, 150)

#### 3.3 绘制平台

创建几个浮动平台：

| 平台 | 位置 | 大小 (瓦片数) |
|------|------|-------------|
| 平台 1 | (-150, 50) | 3x1 |
| 平台 2 | (0, -30) | 3x1 |
| 平台 3 | (150, 50) | 3x1 |

#### 3.4 添加墙壁 (可选)

在关卡两侧添加墙壁防止掉落。

---

### 步骤 4: 配置出生点

#### 4.1 验证出生点

场景中已有两个出生点：
- `SpawnPoint1`: (-300, -100) - 起始位置
- `SpawnPoint2`: (200, -100) - 检查点位置

#### 4.2 添加更多出生点

1. 右键 `Level01` → 创建子节点
2. 选择 `Node2D`
3. 命名为 `SpawnPoint3`
4. 设置位置
5. 添加到 `LevelManager` 的 `spawn_points` 数组

---

### 步骤 5: 配置检查点

#### 5.1 使用预制检查点

1. 在 FileSystem 找到 `scenes/checkpoint.tscn` (需要创建)
2. 拖拽到场景中
3. 设置位置 (如 (200, -50))

#### 5.2 手动创建检查点

1. 创建 `Area2D` 节点
2. 添加脚本 `scripts/checkpoint.gd`
3. 添加 `Sprite2D` 显示检查点标志
4. 添加 `CollisionShape2D` (覆盖检查点区域)

---

### 步骤 6: 添加陷阱

#### 6.1 放置尖刺

1. 找到 `scenes/spike.tscn`
2. 拖拽到关卡中
3. 放在地面或平台上

#### 6.2 尖刺位置示例

| 位置 | 说明 |
|------|------|
| (50, 134) | 地面尖刺 |
| (-100, 134) | 地面尖刺 |
| (300, 134) | 地面尖刺 |

---

### 步骤 7: 配置摄像机

#### 7.1 设置边界

选中 `Camera2D`，设置：

```
Limit Left: -500
Limit Top: -300
Limit Right: 500
Limit Bottom: 300
```

#### 7.2 平滑跟随

```
Position Smoothing: 启用
Smoothing Speed: 5.0
```

---

## 🎮 测试关卡

### 运行测试

1. 确保 `scenes/level_01.tscn` 是打开的
2. 点击运行按钮 (▶️)
3. 测试内容：
   - ✅ 玩家从出生点生成
   - ✅ 可以左右移动
   - ✅ 可以跳跃到平台上
   - ✅ 摄像机跟随玩家
   - ✅ 碰到尖刺会死亡
   - ✅ 死亡后在检查点重生

---

## 📋 关卡设计清单

### 基础元素
- [ ] 地面 (长平台)
- [ ] 起始平台
- [ ] 2-3 个浮动平台
- [ ] 墙壁或边界

### 挑战元素
- [ ] 1-2 个检查点
- [ ] 2-3 个尖刺陷阱
- [ ] 1-2 个敌人 (后续添加)

### 收集物 (可选)
- [ ] 3-5 个金币
- [ ] 1 个特殊道具

### 视觉效果
- [ ] 背景颜色/图片
- [ ] 装饰瓦片 (云朵、树木等)
- [ ] 粒子特效 (后续添加)

---

## 🎯 推荐关卡布局

```
                    [平台 3]
           [平台 2]      👹
     [平台 1]    ⛳
🚩 👤  ──────────────🔺─────🔺──────
   起点    地面      尖刺   尖刺  终点
```

**图例：**
- 👤 = 玩家起点
- 🚩 = 出生点
- ⛳ = 检查点
- 🔺 = 尖刺
- 👹 = 敌人 (后续)
- [平台] = 浮动平台

---

## ⚠️ 常见问题

### Q: 瓦片显示不出来？
A: 检查 TileSet 是否正确关联，Tile Size 是否设为 32x32

### Q: 玩家穿过平台？
A: 确保平台有 CollisionShape2D，collision_layer 设置为 4 (terrain)

### Q: 摄像机不跟随？
A: 检查 Camera2D 是否是玩家的子节点，或脚本中正确设置 target

### Q: 检查点不工作？
A: 确保检查点的 collision_mask 包含玩家层 (1)，玩家要在 "player" 组

---

## 🚀 下一步

关卡基础配置完成后：

1. **添加敌人** - 巡逻敌人 AI
2. **添加收集物** - 金币、道具系统
3. **添加 UI** - 血量、分数显示
4. **设计更多关卡** - level_02, level_03...

告诉我你想做哪个！🦐

---
_最后更新：2026-05-09 | 小虾 🦐_
